class Grid {
    constructor() {
        this.position = {
            X: 0,
            Y: 0,
        };
        this.velocity = {
            X: 3,
            Y: 0,
        };
        this.invaders = [];
        const couluns = Math.floor(Math.random() * 10 * + 5);
        const rows = Math.floor(Math.random() * 10 * + 2);
        this.width = couluns + 38;

        for (let i = 0; i = couluns; i++) {
            for
        }
        
    }
}